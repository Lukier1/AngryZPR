toolsets = gnu vs2010;
program AngryZPR {
    sources { Src/Main.cpp Src/Root.cpp Src/SystemManager.cpp} 
    if ( $(toolset) == gnu )
      	cxx-compiler-options = "-std=c++11";
    if ( $(toolset) == gnu )
	libs = sfml-graphics;
	libs += sfml-window;
	libs += sfml-system;
	libs += Box2D;
}
